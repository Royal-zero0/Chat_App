<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mychatapplogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mychatapplogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyChatApp0"
        android:enableOnBackInvokedCallback="true">
        <activity android:name=".specificchat"></activity>
        <activity android:name=".UpdateProfile" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".chatActivity" />
        <activity android:name=".setProfile" />
        <activity android:name=".otpAuthentication" />
        <activity android:name=".MainActivity"  />
        <activity android:name=".splashscreen"
            android:exported="true">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> >
        </activity>
    </application>

</manifest>